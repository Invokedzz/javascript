/*let numero = 1;
let numero2 = 3;
let numeroGrande = 10.578955;
 console.log(numero.toString() + numero2); // conversao de numero em string "toString()"
 // numero = numero.toString "altera ele para String para sempre";

 console.log(numero.toString(2)); // versao binaria;
 console.log(numeroGrande.toFixed(4)); // analise das casas decimais;
 console.log(Number.isInteger(numeroGrande)) // analisa se o numero eh inteiro ou nao.

let bomba = numero * 'wrong';
console.log(Number.isNaN(bomba)); // analisa se eh um numero ou nao (nan).

let teste = 9.54578;
let analisando = Math.floor(teste); // arredonda para baixo.
let cima = Math.ceil(teste); // arredonda para cima.
let arredondando = Math.round(teste); // Na metade, arredondanda pra cima. Da metade para baixo, arredonda para baixo.

console.log(Math.max(1, 2, 3, 4, 5, 6, 10)); // Pega o maior numero da sequencia.

console.log(Math.min(0, 1, 3, 10, 5, 2)); // Pega o menor numero da sequencia.

console.log(Math.random()); // Pega valores aleatorios.

const aleatorio = Math.round(Math.random() * (10 - 5) + 10);

console.log(Math.pow(2, 10)) // mesma coisa que dois elevado a dez. */





  
   

